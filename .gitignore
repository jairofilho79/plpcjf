.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
.dev.vars
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
*.log
.vscode/
.idea/
dist/
.cache/

# Sem barra final: "/assets/" só casa diretório, e em quem tem `assets` como
# link simbólico (comum: aponta para o acervo fora do repo) isso não pega —
# era só o `.git/info/exclude` local (não compartilhado) que cobria o caso,
# e um clone novo podia acabar commitando os 5305 PDFs por engano.
/assets
/packages/
.plpc-offline-bundle*/
.offline-bundle-*/
plpc-offline-bundle.zip
